# Vercel local link config (auto-generated by `vercel link`)
.vercel

# Secrets — never commit
.vercel-token
.github-token
git-askpass.sh

# Daily archive copies — useful local history, but no need in the repo
archive/

# Previous-day snapshots used by diff_log.py (regenerated each run)
*.previous

# Python bytecode
__pycache__/
*.pyc

# macOS
.DS_Store
test_write.txt
